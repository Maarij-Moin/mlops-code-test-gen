# AI-Driven Code Test Generation (MLOps Project)

## Description
System that generates and updates test cases based on code changes using AI.

## Setup
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload